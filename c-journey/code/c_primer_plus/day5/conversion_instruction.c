#include <stdio.h>
int main()
{
	int a=10, b=20;
	printf("a=%d , b=%d \n",a);
	//wrong situation because the conversion instructions is 
	//not equal with the number of variable.
	//the function will print a casual number which is in memory.
}